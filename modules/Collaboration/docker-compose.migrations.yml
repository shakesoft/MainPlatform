version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Collaboration_Identity
      - Collaboration_DB=Collaboration_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
